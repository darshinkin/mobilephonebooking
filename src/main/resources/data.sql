CREATE TABLE phone (
   id INT AUTO_INCREMENT  PRIMARY KEY,
   exist BOOLEAN,
   model VARCHAR(50),
   brand VARCHAR(50),
   person VARCHAR(50),
   exist availability,
   bookedOut DATE,
   technology VARCHAR(50),
   twoGBang VARCHAR(50),
   threeGBang VARCHAR(50),
   fourGBang VARCHAR(50)
)